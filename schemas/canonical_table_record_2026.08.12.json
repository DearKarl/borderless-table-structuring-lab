{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "borderless-table-structuring/canonical-table-record/2026.08.12",
  "title": "Canonical Table Record 2026.08.12",
  "type": "object",
  "required": [
    "schema_release",
    "sample_id",
    "document_id",
    "split",
    "source_dataset",
    "record_status",
    "source_record",
    "canonical_table",
    "historical_supervision"
  ],
  "properties": {
    "schema_release": {"const": "2026.08.12"},
    "sample_id": {"type": "string", "minLength": 1},
    "document_id": {"type": "string", "minLength": 1},
    "split": {"type": "string", "minLength": 1},
    "source_dataset": {"type": "string", "minLength": 1},
    "record_status": {"enum": ["included", "quarantined"]},
    "source_record": {
      "type": "object",
      "required": ["manifest_path", "manifest_sha256", "line_number", "record_sha256"]
    },
    "canonical_table": {
      "type": "object",
      "required": ["rows", "cols", "cells", "semantic_state_sha256", "full_state_sha256"]
    },
    "explicit_topology_target": {"type": ["object", "null"]},
    "lora_canonical_candidate_target": {"type": ["object", "null"]},
    "historical_supervision": {
      "type": "object",
      "required": ["primary_truth", "historical_action_path_nonunique"]
    }
  },
  "additionalProperties": true
}
